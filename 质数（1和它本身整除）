#include<stdio.h>
#include<stdlib.h>

int main(void)
{
	int j,i = 1;
	for (i = 2; i < 100; i++)
	{
		for (j = 2; j <= i; j++)
		if (i%j == 0)
			break;
		if (i == j)
			printf("%d\t", i);
	}
	system("pause");
	return 0;
}
